const whaleTracker = require('../features/whale');
const wallCheck = require('../dom/walls');

class JohnStrategy {
    evaluate(data) {
        // "Ramuan" Koh John: Whale + Wall Break
        return null;
    }
}
module.exports = new JohnStrategy();
